services:
  baikal:
    image: justadri/baikal-alpine:latest
    container_name: baikal
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      TZ: America/New_York
      # --- mail (pick one) -----------------------------------------
      SMTP_HOST: smtp.example.com
      SMTP_PORT: "587"
      SMTP_USER: baikal@example.com
      SMTP_PASS: changeme
      SMTP_FROM: baikal@example.com
      # MSMTPRC: |
      #   defaults
      #   tls on
      #   account default
      #   host smtp.example.com
      #   ...
    volumes:
      - config:/var/www/baikal/config
      - specific:/var/www/baikal/Specific

  # Uncomment if you want MySQL instead of the default SQLite backend.
  # db:
  #   image: mysql:8.4
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_RANDOM_ROOT_PASSWORD: "yes"
  #     MYSQL_DATABASE: baikal
  #     MYSQL_USER: baikal
  #     MYSQL_PASSWORD: changeme
  #   volumes:
  #     - db:/var/lib/mysql

volumes:
  config:
  specific:
  # db:
